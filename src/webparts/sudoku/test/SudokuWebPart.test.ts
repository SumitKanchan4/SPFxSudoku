/// <reference types="mocha" />

import { assert } from 'chai';
import Sudoku from '../components/Sudoku';

describe('SudokuWebPart', () => {
  it('should do something', () => {
    assert.ok(true);
  });

  
});
